// TODO
const generateWords = async wordsSoFar => {
  if (!wordsSoFar) wordsSoFar = []
  return ['bear', 'boy']
  // TODO: make sure wordsSoFar doesn't contain word
}

const calculateResult = game => {
  // TODO
}

module.exports = { generateWords, calculateResult }